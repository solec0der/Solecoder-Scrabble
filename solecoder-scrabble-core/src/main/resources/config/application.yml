spring:
  profiles:
    active: dev
    include: domain
  application:
    name: Solecoder Scrabble

info:
  app:
    name: Solecoder Scrabble

server:
  port: 9090
  servlet:
    context-path: /solecoder-scrabble-core

keycloak:
  realm: solecoder-scrabble-realm
  resource: solecoder-scrabble-core
  ssl-required: external
  principal-attribute: preffered_username
